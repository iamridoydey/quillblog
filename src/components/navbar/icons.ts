import { Home, Search, NotebookTextIcon, Bookmark, User, Settings } from "lucide-react";

export const icons = {
  home: Home,
  search: Search,
  blogs: NotebookTextIcon,
  savedItems: Bookmark,
  user: User,
  settings: Settings,
};
